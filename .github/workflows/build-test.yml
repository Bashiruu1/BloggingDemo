name: build-and-test

on:
  push:
    branches:
      - "*"
  pull_request:
    branches:
      - "*"

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2
      - name: Build and test
        run: |
          docker build -t myapp:latest .
          docker run myapp:latest dotnet test
      - name: Login to Docker Hub
        uses: docker/login-action@v1
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}
      - name: Push to Docker Hub
        run: |
          echo ::set-env name=IMAGE_TAG::${GITHUB_REF#refs/heads/}-$(echo $GITHUB_RUN_NUMBER)
          docker tag myapp:latest <dockerhub-username>/myapp:${{ env.IMAGE_TAG }}
          docker push <dockerhub-username>/myapp:${{ env.IMAGE_TAG }}
